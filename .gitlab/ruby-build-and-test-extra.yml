###############################################################################
# Copyright (c) 2022, Lawrence Livermore National Security, LLC and RADIUSS
# project contributors. See the COPYRIGHT file for details.
#
# SPDX-License-Identifier: (MIT)
###############################################################################

########################
# Overridden shared jobs
########################
# We duplicate the shared jobs description and add necessary changes for RAJA.
# We keep ${PROJECT_<MACHINE>_VARIANTS} and ${PROJECT_<MACHINE>_DEPS} So that
# the comparison with the original job is easier.

# No overridden jobs so far.

############
# Extra jobs
############
# We do not recommend using ${PROJECT_<MACHINE>_VARIANTS} and
# ${PROJECT_<MACHINE>_DEPS} in the extra jobs. There is not reason not to fully
# describe the spec here.

gcc_10_2_1_numa:
  variables:
    SPEC: "~shared +fortran +numa ~device_alloc +tools tests=basic %gcc@10.2.1 ^blt@91799350f002307c22a3d079abdf8ed37a6cbb2f"
  extends: .build_and_test_on_ruby

clang_14_0_4_sqlite_experimental:
  variables:
    SPEC: "~shared +sqlite_experimental ~device_alloc +tools tests=basic %clang@14.0.4 ^blt@91799350f002307c22a3d079abdf8ed37a6cbb2f"
  extends: .build_and_test_on_ruby

# Develop builds against specific tpl version.
pgi_22_1_gcc_local_8_3_1_tpls:
  variables:
    SPEC: "~shared +fortran ~device_alloc +tools tests=basic %pgi@22.1.gcc.local.8.3.1 ^blt@91799350f002307c22a3d079abdf8ed37a6cbb2f"
  extends: .build_and_test_on_ruby

clang_14_0_4_tpls:
  variables:
    SPEC: "~shared +fortran ~device_alloc +tools tests=basic %clang@14.0.4 ^blt@91799350f002307c22a3d079abdf8ed37a6cbb2f"
  extends: .build_and_test_on_ruby

gcc_10_2_1_tpls:
  variables:
    SPEC: "~shared +fortran ~device_alloc +tools tests=basic %gcc@10.2.1 ^blt@91799350f002307c22a3d079abdf8ed37a6cbb2f"
  extends: .build_and_test_on_ruby

